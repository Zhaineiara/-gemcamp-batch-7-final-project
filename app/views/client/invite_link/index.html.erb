<div class="container mt-5 mb-5">
  <div class="row justify-content-center">
    <div class="col-md-6 text-center">
      <!-- QR Code Section -->
      <div class="mb-4">
        <div class="p-4 border border-primary rounded m-auto">
          <%= @qrcode_svg.html_safe %>
        </div>
      </div>

      <!-- Signup Link Section -->
      <div class="alert alert-info mb-3">
        <h6 class="text-truncate text-wrap"><%= request.base_url + new_client_user_registration_path(promoter: current_client_user.email)%></h6>
      </div>
    </div>
  </div>
</div>