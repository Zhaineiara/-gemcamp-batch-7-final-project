<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <b><%= link_to 'LOTTERY SYSTEM', client_home_index_path, class: 'navbar-brand' %></b>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav ms-auto me-5">
        <li class="nav-item">
          <%= link_to client_shop_index_path, class: "nav-link active d-flex align-items-center", style: "font-size: 1rem; color: #333; text-decoration: none;" do %>
            <i class="bi bi-coin" style="font-size: 1.2rem; color: #f9a602; margin-right: 0.5rem;"></i>
            <span>
              <%= @user_coins || "Coins" %>
            </span>
          <% end %>
        </li>

        <li class="nav-item">
          <%= link_to client_winnings_path, class: "nav-link active d-flex align-items-center position-relative hover-claim", style: "font-size: 1rem; color: #333; text-decoration: none;" do %>
            <i class="bi bi-gift me-2 position-relative" style="font-size: 1.3rem; color: #f9a602; margin-right: 0.5rem; transition: transform 0.3s ease;">
                <span class="position-absolute badge bg-danger" style="top: -10px; right: -10px; font-size: 0.7rem; padding: 0.2em 0.4em; border-radius: 50%;">
                  <%= @won_count || 0 %>
                </span>
            </i>
          <% end %>
        </li>

        <li class="nav-item dropdown">
          <a
            class="nav-link active d-flex align-items-center me-1"
            href="#"
            id="languageDropdown"
            role="button"
            data-bs-toggle="dropdown"
            aria-expanded="false"
            style="font-size: 1rem; color: #333;"
          >
            <i class="bi bi-translate me-2" style="font-size: 1.2rem; color: #f9a602; margin-right: 0.5rem;"></i>
            <span>Language</span>
          </a>
          <ul class="dropdown-menu" aria-labelledby="languageDropdown">
            <li><a class="dropdown-item" href="#">English</a></li>
            <li><a class="dropdown-item" href="#">Spanish</a></li>
            <li><a class="dropdown-item" href="#">French</a></li>
          </ul>
        </li>

        <% if client_user_signed_in? %>
          <li class="nav-item">
            <%= button_to 'Sign Out', destroy_client_user_session_path, method: :delete, class: 'btn btn-danger px-4 py-2 rounded-pill', data: { confirm: 'Are you sure you want to sign out?' } %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'LOGIN', new_client_user_session_path, class: "btn btn-outline-success px-4 py-2 rounded-pill #{'active' if current_page?(new_client_user_session_path)}" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

<style>
    .hover-claim:hover i {
        transform: scale(1.1);
    }
</style>